export type AuthMode = "login" | "signup";

export type ProfileFormValues = {
  fullName: string;
  email: string;
  age: string;
  city: string;
  bio: string;
  gender: string;
  preferences: string;
};
